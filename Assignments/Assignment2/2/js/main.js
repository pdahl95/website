// JavaScript File for exersice 2

function addAndPrintInConsole(x, y){
    console.log(x + "+" + y + "=" + (x+y));
}

function addAndPrintInDocument(x, y){
    document.write("<p>" + x + "+" + y + "=" + (x+y) + "</p>");
}
